<%  if (answers.framework === 'mocha') { %>
    mochaOpts: {
        ui: 'bdd',
        timeout: 60000
    },
<% } else if (answers.serenityAdapter === 'mocha') { %>
    mochaOpts: {
        timeout: 60000
    },
<% } %>
