<% if (reporters.length) { -%>
    reporters: [<%- reporters.map(reporter => {
        if (reporter === 'allure') {
            return `['${reporter}', {outputDir: 'allure-results'}]`
        } else {
            return `'${reporter}'`
        }
    }) -%>],
<% } else { -%>
<% } -%>
