/**
 *
 * Switch to a particular tab, window or frame.
 *
 * @param {String=} target  target window to switch to
 * @uses protocol/window, window/getTabIds, window/switchTab
 * @type window
 *
 */

let switchTab = function () {
    // ToDo: implement command
}

export default switchTab
